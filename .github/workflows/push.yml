name: actions-test
on:
  push:

jobs:
  setup:
    uses: actions/checkout@v3

  ubuntu:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Sneha Test
        run: make hello && ./hello

  mac:
    runs-on: macos-latest
    needs: setup
    steps:
      - name: Sneha Test
        run: make hello && ./hello
